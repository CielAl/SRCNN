# import tables
# class dataLoader():

	# def __init__(self,fileloc):
		# self.fileloc = database.export_dir
	# '''
		# Slice the chunk of patches out of the database.
		# Precondition: the pytable must be written.
		# Args:
			# index: (chunk index)
	# '''
	# def __getitem__(self, index):
		# with tables.open_file(self.fileloc,'r') as pytable:
            # setattr(self,index[0])=getattr(pytable.root,index[0])
		
  